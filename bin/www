#!/usr/bin/env node

"use strict";

var http   = require("http");
var app    = require("../app");
var port   = process.env.PORT || 3000;
var addr   = process.env.INTERFACE || "localhost";
var server = app.listen(port, addr);

server.on("listening", function () {
  console.log("Listening on http://" + addr + ":" + port);
});
